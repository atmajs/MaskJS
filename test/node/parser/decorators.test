UTest({
	'should parse and serialize method' () {

		var ast = Mask.parse(`
			[Colorize]
			div;
		`);

		var nodes = ast.nodes;
		eq_(nodes.length, 2);

		eq_(nodes[0].expression, 'Colorize');
		eq_(nodes[0].type, Mask.Dom.DECORATOR);

		eq_(nodes[1].tagName, 'div');
		eq_(nodes[1].type, Mask.Dom.NODE);

		'> serialize'

		var str = Mask.stringify(ast);
		eq_(str, '[Colorize]div;')
	}
})